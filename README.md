### Unable to build

Run

```
npx nx serve main
```

```
ERROR in ./src/styles.scss
  ? Module build failed:
  ???   ? Can't find stylesheet to import.
        ?   ?
        ? 2 ? @import 'core/global';
        ?   ?         ^^^^^^^^^^^^^
        ?   ?
```
